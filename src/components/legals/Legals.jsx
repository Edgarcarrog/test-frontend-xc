import React from "react";

const Legals = ({ data }) => {
  return (
    <div className="col-start-2 col-span-9 mt-[37px] mb-[19px] text-base">
      {data}
    </div>
  );
};

export default Legals;
